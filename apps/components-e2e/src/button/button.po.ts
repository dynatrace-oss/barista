import { Selector } from 'testcafe';

export const button = Selector('#test-button');
export const disableButton = Selector('#disable-toggle');
export const clickCounter = Selector('#click-counter');
