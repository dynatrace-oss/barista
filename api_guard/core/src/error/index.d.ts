export declare const DT_COMPARE_WITH_NON_FUNCTION_VALUE_ERROR_MSG =
  '`compareWith` must be a function.';

export declare class ErrorStateMatcher {
  isErrorState(
    control: FormControl | null,
    form: FormGroupDirective | NgForm | null,
  ): boolean;
}
