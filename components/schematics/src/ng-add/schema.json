{
  "$schema": "http://json-schema.org/schema",
  "id": "NgAddOptionsSchema",
  "type": "object",
  "description": "Schema for Ng add options",
  "properties": {
    "animations": {
      "type": "boolean",
      "description": "Installation of @angular/animations",
      "default": true,
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "x-prompt": "Install @angular/animations?"
    },
    "stylesPack": {
      "type": "boolean",
      "description": "Configure which stylePack should be installed.",
      "default": true,
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "x-prompt": "Install complete stylepack (no, will install minimum stylepack)"
    },
    "iconpack": {
      "type": "boolean",
      "description": "Installation of @dynatrace/barista-icons",
      "default": true,
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "x-prompt": "Install @dynatrace/barista-icons"
    },
    "angularPathRefactor": {
      "type": "boolean",
      "description": "Update of angular.json paths",
      "defualt": true,
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "x-prompt": "Refactor paths in angular json? (for Dynatrace Fonts and Icons)"
    }
  },
  "required": ["animations", "iconpack", "angularPathRefactor"]
}
