{
  "$schema": "http://json-schema.org/schema",
  "id": "package-element",
  "type": "object",
  "properties": {
    "entryFile": {
      "type": "string",
      "description": "The entrypoint used for the rollup build."
    },
    "project": {
      "type": "string",
      "description": "The path to the package.json used for the release."
    },
    "outputPath": {
      "type": "string",
      "description": "The path where the elements target should build to."
    },
    "packageVersion": {
      "type": "string",
      "description": "Package version that is used to release"
    }
  },
  "additionalProperties": false,
  "required": ["entryFile", "project", "outputPath"],
  "definitions": {}
}
