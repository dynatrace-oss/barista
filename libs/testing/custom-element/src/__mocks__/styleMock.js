// Mock css module that is injected by the libs/testing/custom-element/jest.config.js
// as a default dummy style, so the jest ts transformer can compile correctly.
// Styles are probably not needed in the unit-tests, which makes this solution
// applicable for now.
module.export = {};
