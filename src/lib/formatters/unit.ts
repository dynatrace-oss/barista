export const enum DtUnit {
  PERCENT = '%',
  COUNT = 'count',
  BYTES = 'B',
  KILO_BYTES = 'kB',
  MEGA_BYTES = 'MB',
  GIGA_BYTES = 'GB',
  TERA_BYTES = 'TB',
  PETA_BYTES = 'PB',
}

export const enum DtRateUnit {
  PER_SECOND = 's',
  PER_MINUTE = 'min',
}
