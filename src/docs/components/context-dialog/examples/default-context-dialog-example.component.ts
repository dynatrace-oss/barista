import { Component } from '@angular/core';

@Component({
  moduleId: module.id,
  template: `<dt-context-dialog>
  <button dt-button variant="secondary">Edit</button>
  <button dt-button variant="secondary">Add</button>
  </dt-context-dialog>`,
})
export class DefaultContextDialogExampleComponent { }
